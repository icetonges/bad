import Link from 'next/link'
import { ArrowRight, FileSpreadsheet, Gavel, Calculator, FileCheck, Bot, Database } from 'lucide-react'

export default function Landing() {
  return (
    <div className="min-h-screen">
      <nav className="border-b border-border">
        <div className="container flex h-14 items-center justify-between">
          <Link href="/" className="font-medium text-sm tracking-tight">fedAnalyst</Link>
          <div className="flex items-center gap-4 text-sm">
            <Link href="/dashboard" className="text-muted-foreground hover:text-foreground transition">Dashboard</Link>
            <Link href="/dashboard/chat" className="text-muted-foreground hover:text-foreground transition">Ask the agent</Link>
            <Link href="/login" className="rounded-md bg-primary text-primary-foreground px-3 py-1.5 text-xs font-medium">Sign in</Link>
          </div>
        </div>
      </nav>

      <section className="container py-20 md:py-32">
        <div className="max-w-3xl">
          <p className="text-xs font-medium tracking-wider uppercase text-muted-foreground mb-4">For federal government analysts</p>
          <h1 className="text-4xl md:text-5xl font-medium tracking-tight leading-tight mb-6">
            An AI analyst for budget, audit, accounting, and contract work.
          </h1>
          <p className="text-lg text-muted-foreground leading-relaxed mb-8">
            Upload the documents you already work with — PB justifications, audit reports, financial statements, contract files. Get dashboards, insider analysis, and standard reporting generated by an agent that understands how federal programs actually work.
          </p>
          <div className="flex flex-wrap gap-3">
            <Link href="/dashboard" className="inline-flex items-center gap-2 rounded-md bg-primary text-primary-foreground px-4 py-2 text-sm font-medium">
              Open dashboard <ArrowRight className="h-4 w-4" />
            </Link>
            <Link href="/dashboard/chat" className="inline-flex items-center gap-2 rounded-md border border-border px-4 py-2 text-sm">
              Talk to the agent
            </Link>
          </div>
        </div>
      </section>

      <section className="container pb-20">
        <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4">
          <CategoryCard href="/dashboard/budget" icon={FileSpreadsheet} title="Budget" copy="PB justifications, appropriation analysis, mandatory vs discretionary, MAC munitions." />
          <CategoryCard href="/dashboard/audit" icon={Gavel} title="Audit" copy="GAO findings, IG reports, unmodified opinion readiness, material weakness tracking." />
          <CategoryCard href="/dashboard/accounting" icon={Calculator} title="Accounting & data" copy="Financial statements, obligations, outlays, unliquidated obligations, cost reports." />
          <CategoryCard href="/dashboard/contracts" icon={FileCheck} title="Contracts" copy="FAR/DFARS compliance, contract reviews, cost analysis, performance monitoring." />
        </div>
      </section>

      <section className="border-t border-border bg-muted/30">
        <div className="container py-16">
          <h2 className="text-2xl font-medium tracking-tight mb-8">How the agent works</h2>
          <div className="grid gap-6 md:grid-cols-3">
            <Feature icon={Database} title="Knowledge base" copy="Your uploaded documents are chunked, embedded, and served back to the agent via vector search so answers are grounded in your source material." />
            <Feature icon={Bot} title="Skills and tools" copy="Each domain (budget, audit, accounting, contracts) is a skill with its own prompts and output schemas. The agent uses tools — web search, Python, MCP servers — to compose answers." />
            <Feature icon={FileCheck} title="Standard reporting" copy="One-click generation of the report formats you already produce: budget briefings, audit summaries, contract cost analyses, dashboards." />
          </div>
        </div>
      </section>

      <footer className="border-t border-border">
        <div className="container py-8 text-xs text-muted-foreground flex flex-wrap gap-4 justify-between">
          <p>fedAnalyst — not affiliated with any federal agency. Do not upload classified or CUI material without appropriate authorization.</p>
          <Link href="#contact" className="hover:text-foreground">Contact</Link>
        </div>
      </footer>
    </div>
  )
}

function CategoryCard({ href, icon: Icon, title, copy }: { href: string; icon: any; title: string; copy: string }) {
  return (
    <Link href={href} className="group rounded-lg border border-border bg-card p-5 transition hover:border-foreground/30">
      <Icon className="h-5 w-5 mb-3 text-muted-foreground group-hover:text-foreground transition" />
      <h3 className="font-medium text-sm mb-1">{title}</h3>
      <p className="text-xs text-muted-foreground leading-relaxed">{copy}</p>
    </Link>
  )
}

function Feature({ icon: Icon, title, copy }: { icon: any; title: string; copy: string }) {
  return (
    <div>
      <Icon className="h-5 w-5 mb-3 text-muted-foreground" />
      <h3 className="font-medium text-sm mb-2">{title}</h3>
      <p className="text-sm text-muted-foreground leading-relaxed">{copy}</p>
    </div>
  )
}
